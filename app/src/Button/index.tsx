import { Text, View } from "react-native";
import { styles } from "./styles";

export const Button = () => {
  return (
    <View style={styles.container}>
      <Text>Button </Text>
    </View>
  );
};
