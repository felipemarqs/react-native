import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderRadius: 10,
    borderColor: "#fff",
    height: 48,
    paddingHorizontal: 16,
    color: "#fff",
  },
  inputFocused: {borderColor: "#EAE6E5"},
});
